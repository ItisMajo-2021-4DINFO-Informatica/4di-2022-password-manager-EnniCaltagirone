<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Notes.Views.AboutPage"
             Title="Info">
    <!-- Layout children in two rows -->
    <Grid RowDefinitions="Auto,*">
        <!-- Layout children vertically -->
        <StackLayout Grid.Row="1"
                     Margin="20"
                     Spacing="20">
            <Label FontSize="22">
                <Label.FormattedText>
                    <FormattedString>
                        <FormattedString.Spans>
                            <Span Text="Gestore password"
                                  FontAttributes="Bold"
                                  FontSize="30" />
                            <Span Text=" v1.0" />
                        </FormattedString.Spans>
                    </FormattedString>
                </Label.FormattedText>
            </Label>
            <Label Text="Questa è un app molto utile per salvare le tue pasword, in modo sicuro potendo impostare una password diversa per ogni servizio, senza aver paura di dimenticarle" />
            <Button Text="Conosci il creatore"
                    Clicked="OnButtonClicked" />
        </StackLayout>
    </Grid>
</ContentPage>